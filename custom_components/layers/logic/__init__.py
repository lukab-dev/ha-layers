"""Pure decision logic for Layers.

Nothing in this package may import Home Assistant. It must run on Python 3.12+
on its own, so the same code that decides in Home Assistant can be unit-tested
without it and replayed against recorded history.
"""
